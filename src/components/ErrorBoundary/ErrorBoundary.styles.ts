const styles = {
  wrapper: {
    width: "100vw",
    height: "100vh",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  },
  icon: { fontSize: "4rem", color: "error.main" },
} as const;

export default styles;
